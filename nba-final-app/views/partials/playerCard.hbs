<div class="col-md-4">
    <div class="card player-card">
        <img src="{{photo}}" alt="{{name}} {{lastName}}" class="card-img-top">
        <div class="card-body">
            <h5 class="card-title">{{name}} {{lastName}}</h5>
            <p class="card-text">{{team}}</p>
        </div>
    </div>
</div>